# Public project ingestion key, never a PostHog personal API key.
VITE_PUBLIC_POSTHOG_KEY=
# Use the host matching the project's region (EU or US).
VITE_PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com
# build:web defaults to web. Desktop release builds must set their channel.
VITE_PUBLIC_DISTRIBUTION=
